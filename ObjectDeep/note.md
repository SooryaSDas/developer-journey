1. how to get all property names:
const user = {
    name: "Soorya",
    age: 28,
    city: "Kochi"
};

console.log(Object.keys(user));

2. find key
Object.keys(user);

3. find values
Object.values(user);